
<% @templates.each do |t| %>
  <div>
    <%= link_to t.name, "/manipulator_templates/#{t.id}" %>
    <%= link_to 'vars', "/variable_properties?template_id=#{t.id}" %>
    <%= t.kind %>
  </div>
<% end %>

<br/>
<br/>
<%= link_to "New Template", '/manipulator_templates/new' %>
