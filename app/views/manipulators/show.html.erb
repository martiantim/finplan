<%= form_for(@manipulator, :html => {}) do |f| %>
  <div>Name: <%= f.text_field :name, :size => 20 %></div>  
  
  <h4>Variables</h4>
  <% @template.params.each do |var|  %>
    <div><%= var%>:<input type="text" name="variables[<%=var%>]" value="<%=@manipulator.param_value(var)%>"></input></div> 
  <% end %>

  <%= f.submit "Save" %>
<% end %>